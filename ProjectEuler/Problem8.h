#pragma once

#include <string>

/// <summary>
/// Finds the maximal product of K consecutive of an N-digit number.
/// </summary>
class Problem8 {

public:
	static int Solve(const std::string& nbStr, int consecutive);
};
#pragma once

/// <summary>
/// A pythagorean triplet is one where, if a < b < c, a^2 + b^2 = c^2
/// Given N, find if a pythagorean triplet exists such that a + b + c = N
/// and return the highest product a*b*c from those triplets
/// Return -1 otherwise
/// </summary>
class Problem9 {
public:
	static int Solve(int N);
};